import 'package:flutter/material.dart';

class Archive extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('This is archive'),
      ),
      body: Center(
        child: Text('This is the archive page!'),
      ),
    );
  }
}
