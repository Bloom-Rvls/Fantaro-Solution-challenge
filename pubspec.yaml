name: namer_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0
  material: ^1.0.0+2
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_native_splash: ^2.2.19

flutter_native_splash:
  color: "#ffffff"
  #background_image: "assets/images/Logo FANTARO.jpeg"
  image: "assets/images/Logo FANTARO.jpeg"
  flutter_lints: ^2.0.0

flutter:    
  uses-material-design: true
  assets:
    - assets/images/logoFantaroBlack-removebg.png
